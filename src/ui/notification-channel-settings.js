(function(){
  'use strict';
  const K=window.KCDP=window.KCDP||{};
  let settingsWindowUntil=0;
  const allowed=()=>K.notificationChannelSafety?.allowedAdmin?.()===true;
  function close(){document.getElementById('kcNotificationChannelOverlay')?.remove()}
  function statusCard(name,label){const s=K.notificationChannelSafety.status(name);const live=s.live;return `<section style="border:1px solid #d8d8d8;border-radius:14px;padding:16px;margin:12px 0;background:${live?'#fff7e6':'#f1fbf4'}"><div style="display:flex;justify-content:space-between;gap:14px;align-items:center"><div><strong style="font-size:1.1rem">${label}</strong><div style="margin-top:5px;font-weight:700;color:${live?'#a65a00':'#227a3a'}">${live?'🟡 LIVE – Versand aktiv':'🟢 TEST – kein Versand'}</div><div style="font-size:.9rem;margin-top:5px;color:#555">${live?'Automatische Nachrichten dürfen über diesen Kanal gesendet werden.':'Es werden nur Vorschauen erzeugt; echter Versand ist gesperrt.'}</div></div><button type="button" data-channel="${name}" data-next="${live?'test':'live'}" style="min-width:150px;padding:12px;border:0;border-radius:10px;background:${live?'#343434':'#8f1829'};color:#fff;font-weight:700">${live?'Auf TEST stellen':'LIVE freigeben'}</button></div></section>`}
  function render(){const host=document.getElementById('kcNotificationChannelOverlay');if(!host)return;host.innerHTML=`<div style="width:min(680px,94vw);max-height:90vh;overflow:auto;background:#fff;border-radius:18px;padding:20px;box-shadow:0 18px 60px #0005;color:#222"><div style="display:flex;justify-content:space-between;gap:12px;align-items:flex-start"><div><h2 style="margin:0">Versandsteuerung</h2><p style="margin:.35rem 0 0;color:#555">Push und E-Mail werden getrennt freigegeben. Standard ist immer TEST.</p></div><button id="kcNcsClose" type="button" style="border:0;background:#eee;border-radius:9px;padding:8px 11px">✕</button></div>${statusCard('push','Push-Nachrichten')}${statusCard('email','E-Mail-Nachrichten')}<div style="border-top:1px solid #ddd;margin-top:16px;padding-top:14px;font-size:.9rem;color:#555"><b>Sicherheitsregel:</b> LIVE kann nur von einem Admin aktiviert werden. Zum Aktivieren muss zusätzlich das Wort <b>LIVE</b> bestätigt werden. TEST sperrt den echten Versand sofort.</div></div>`;host.querySelector('#kcNcsClose').onclick=close;host.querySelectorAll('[data-channel]').forEach(btn=>btn.onclick=()=>change(btn.dataset.channel,btn.dataset.next));}
  async function change(channel,next){if(next==='test'){K.notificationChannelSafety.setMode(channel,'test');render();return;}const label=channel==='push'?'Push':'E-Mail';const ok=window.confirm(`${label} wirklich auf LIVE stellen?\n\nAb dann dürfen automatische Nachrichten über diesen Kanal versendet werden.`);if(!ok)return;const typed=window.prompt(`Zur Sicherheitsfreigabe bitte LIVE eingeben:`,'');if(String(typed||'').trim().toUpperCase()!=='LIVE'){window.alert('LIVE wurde nicht bestätigt. Der Kanal bleibt im TEST-Modus.');return;}K.notificationChannelSafety.setMode(channel,'live',{confirmation:typed});render();}
  function open(){if(!allowed())return;close();const host=document.createElement('div');host.id='kcNotificationChannelOverlay';host.style.cssText='position:fixed;inset:0;z-index:99999;background:#0008;display:grid;place-items:center;padding:14px';host.onclick=e=>{if(e.target===host)close()};document.body.appendChild(host);render();}
  function inject(){if(!allowed()||Date.now()>settingsWindowUntil)return;const modal=document.getElementById('modal'),back=document.getElementById('modalBackdrop');if(!modal||back?.classList.contains('hidden')||document.getElementById('kcNotificationChannelSettingsEntry'))return;const b=document.createElement('button');b.id='kcNotificationChannelSettingsEntry';b.type='button';b.className='tool-btn';b.style.marginTop='10px';b.textContent='✉ Versandsteuerung TEST / LIVE';b.onclick=open;modal.appendChild(b)}
  function install(){document.getElementById('settingsBtn')?.addEventListener('click',()=>{settingsWindowUntil=Date.now()+2500;setTimeout(inject,80);setTimeout(inject,350)});new MutationObserver(inject).observe(document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['class']});}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
  K.notificationChannelSettingsUi={version:'0.19.56',open,allowed};
})();
