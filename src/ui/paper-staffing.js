(function(){
'use strict';const K=window.KCDP,esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),tm=h=>String(Math.floor(h)).padStart(2,'0')+':'+String(Math.round(h%1*60)).padStart(2,'0');
function sections(day){const raw=K.demandMatrix?.[day.date]||[],points=[...new Set([day.start,day.end,...raw.flatMap(x=>[+x.start,+x.end]),...Array.from({length:Math.ceil(day.end-day.start)},(_,i)=>day.start+i)])].filter(n=>n>=day.start&&n<=day.end).sort((a,b)=>a-b),out=[];for(let i=1;i<points.length;i++){const r=K.wishDemand.requirement(day.date,(points[i-1]+points[i])/2)||{},last=out.at(-1),value={start:points[i-1],end:points[i],front:r.front??null,back:r.back??null,total:r.total??null};if(last&&last.end===value.start&&last.front===value.front&&last.back===value.back&&last.total===value.total)last.end=value.end;else out.push(value)}return out;}
function html(dates=K.days.map(d=>d.date)){
 return `<!doctype html><html lang="de"><head><meta charset="utf-8"><title>KC DP2 Besetzungsmatrix für Papierplanung</title><style>@page{size:A4 landscape;margin:10mm}*{box-sizing:border-box}body{margin:0;font:11pt Arial,sans-serif;color:#111}section{page-break-after:always}section:last-child{page-break-after:auto}h1{font-size:17pt;margin:0 0 3mm}h2{font-size:12pt;margin:4mm 0 2mm}p{margin:2mm 0}table{width:100%;border-collapse:collapse;table-layout:fixed}th,td{border:1px solid #777;padding:2mm;text-align:left}th{background:#eee;font-size:10pt}td{height:8mm}small{font-size:9pt}.note{border-bottom:1px solid #999;height:10mm}.controls{padding:16px;background:#eee}@media print{.controls{display:none}}</style></head><body><div class="controls"><button onclick="window.print()">Drucken / als PDF speichern</button><p>Leere Planungsvorlage. Die Sollzahlen stammen aus der aktuell geladenen Besetzungsmatrix.</p></div>${K.days.filter(d=>dates.includes(d.date)).flatMap(d=>{const rows=sections(d),pages=[];for(let i=0;i<rows.length;i+=6){pages.push(`<section><h1>Besetzungsmatrix · ${esc(d.date)} · ${tm(d.start)}–${tm(d.end)}</h1><p>${esc(K.eventConfig?.name||'KC DP2')} · Papierplanung · Stand ${new Date().toLocaleDateString('de-DE')}</p><small>V = vorne · H = hinten · Z = Sonderfunktion (zählt nicht zu V/H). Namen und Zeiten zunächst von Hand eintragen, später nach DP2 übertragen.</small><h2>Soll-Besetzung und handschriftliche Zuordnung</h2><table><colgroup><col style="width:13%"><col style="width:6%"><col style="width:6%"><col style="width:7%"><col style="width:34%"><col style="width:34%"></colgroup><thead><tr><th>Zeitraum</th><th>Soll V</th><th>Soll H</th><th>Gesamt</th><th>Namen vorne</th><th>Namen hinten</th></tr></thead><tbody>${rows.slice(i,i+6).map(r=>`<tr><td>${tm(r.start)}–${tm(r.end)}</td><td>${r.front??'–'}</td><td>${r.back??'–'}</td><td>${r.total??'–'}</td><td></td><td></td></tr>`).join('')}</tbody></table><h2>Dienste vormerken</h2><table><colgroup><col style="width:27%"><col style="width:12%"><col style="width:12%"><col style="width:10%"><col style="width:14%"><col style="width:25%"></colgroup><thead><tr><th>Name</th><th>Von</th><th>Bis</th><th>V / H / Z</th><th>Nur wenn nötig</th><th>Bemerkung / Bereitschaft</th></tr></thead><tbody>${Array.from({length:7},()=>'<tr><td></td><td></td><td></td><td></td><td>☐</td><td></td></tr>').join('')}</tbody></table><p class="note">Notizen:</p><small>${esc(d.date)} · Blatt ${Math.floor(i/6)+1} von ${Math.ceil(rows.length/6)} · Noch kein freigegebener Dienstplan</small></section>`)}return pages}).join('')}</body></html>`;
}
function open(){if(!K.auth?.has('roster.plan.edit'))throw Error('Die Papierplanung ist für Planer vorgesehen.');const w=window.open('','_blank');if(!w){alert('Bitte das Druckfenster im Browser zulassen.');return;}w.document.write(html());w.document.close();}
K.paperStaffing={html,sections,open};
})();
