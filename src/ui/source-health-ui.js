(function(){
  const K=window.KCDP=window.KCDP||{};
  let scheduled=false,last='';
  const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const diagOpen=()=>!!document.getElementById('kcDiagOverlay');
  function badge(text,state){return `<span class="source-health-badge ${state}">${esc(text)}</span>`;}
  function autoLabel(a){if(!a.enabled)return {text:'aus',state:'local'};if(a.status==='ready')return {text:a.persisted?'bereit · gespeichert':'bereit',state:'ok'};if(a.status==='blocked')return {text:'blockiert',state:'warn'};if(a.status==='error')return {text:'Fehler',state:'bad'};if(a.status==='syncing')return {text:'läuft',state:'warn'};if(a.status==='skipped')return {text:'übersprungen',state:'local'};return {text:'aktiv',state:'local'};}
  function ensureMailSettingsTab(){if(diagOpen())return;const tabs=document.querySelector('#modal .settings-tabs');if(!tabs||tabs.querySelector('[data-kc-mail-tab]'))return;const b=document.createElement('button');b.type='button';b.dataset.kcMailTab='1';b.textContent='E-Mail & Posteingang';b.title='Provider, Empfang, Zugangsdaten und automatische Wunschplan-Verarbeitung';b.onclick=()=>{if(K.emailCenter?.open)K.emailCenter.open();else{loadEmailCenter();setTimeout(()=>K.emailCenter?.open?.(),80)}};tabs.appendChild(b)}
  function refresh(){scheduled=false;if(diagOpen())return;ensureMailSettingsTab();if(!K.sourceHealth?.snapshot)return;const host=document.querySelector('.plan-inspector');if(!host)return;const s=K.sourceHealth.snapshot(),sig=JSON.stringify({day:s.day,overall:s.overall,ps:s.people.status,pSrc:s.people.source,pr:s.people.records,ms:s.manager.status,mSrc:s.manager.source,mBlock:s.manager.block,auto:s.autoSync,ctx:s.context.status,w:s.context.weather.source,p:s.context.program.count});if(sig===last&&host.querySelector('#sourceHealthCard'))return;last=sig;let card=host.querySelector('#sourceHealthCard');if(!card){card=document.createElement('div');card.id='sourceHealthCard';card.className='inspector-card source-health-card';const first=host.querySelector('.inspector-card');first?.insertAdjacentElement('beforebegin',card);if(!first)host.appendChild(card);}const peopleState=s.people.authoritative?'ok':s.people.status==='blocked'||s.people.status==='error'?'bad':'local',managerState=s.manager.ready?'ok':s.manager.partial?'warn':'local',contextState=s.context.status==='manager'?'ok':s.context.status==='partial'?'warn':'local',auto=autoLabel(s.autoSync),managerReason=String(s.manager.block?.reason||'').trim();card.innerHTML=`<small>Datenquellen</small><b>${esc(K.sourceHealth.label(s))}</b><div class="source-health-row"><span>Personen</span>${badge(s.people.authoritative?'KC-Core/Manager':`Lokal · ${s.people.records}` ,peopleState)}</div><div class="source-health-row"><span>Manager-Sync</span>${badge(s.manager.ready?'bereit':s.manager.partial?'teilweise':'nicht führend',managerState)}</div><div class="source-health-row"><span>Auto-Sync</span>${badge(auto.text,auto.state)}</div><div class="source-health-row"><span>Wetter / Programm</span>${badge(s.context.status==='manager'?'Manager':s.context.status==='partial'?'teilweise':'lokal',contextState)}</div>${managerReason&&s.manager.partial?`<div class="source-health-note">Manager-Sync: ${esc(managerReason)}</div>`:''}${s.autoSync.lastError?`<div class="source-health-note">Auto-Sync: ${esc(s.autoSync.lastError)}</div>`:''}`;card.dataset.sourceOverall=s.overall;card.dataset.sourceDay=s.day||'';card.dataset.autoSync=s.autoSync.status;}
  function schedule(){if(diagOpen()||scheduled)return;scheduled=true;requestAnimationFrame(refresh);}
  new MutationObserver(m=>{if(!diagOpen()&&m.some(x=>x.type==='childList'))schedule();}).observe(document.documentElement,{childList:true,subtree:true});
  document.addEventListener('click',e=>{if(e.target?.id==='prevDayBtn'||e.target?.id==='nextDayBtn'||e.target?.closest?.('[data-jump-date]')){last='';setTimeout(schedule,0);}});
  window.addEventListener('KC_DP_MANAGER_AUTO_SYNC',()=>{if(diagOpen())return;last='';schedule();});
  K.sourceHealthUi={version:'0.19.65-no-diag-loader',refresh(){if(diagOpen())return;last='';schedule();}};schedule();
  function script(id,src,parent=document.head){if(document.getElementById(id))return null;const s=document.createElement('script');s.id=id;s.src=src;s.async=false;parent.appendChild(s);return s}
  function css(id,href){if(document.getElementById(id))return;const c=document.createElement('link');c.id=id;c.rel='stylesheet';c.href=href;document.head.appendChild(c)}
  function loadPushCenter(){if(document.getElementById('kcPushCenterCss')||window.KCDP?.pushCenter)return;css('kcPushCenterCss','src/ui/push-center.css?v=0.19.43');script('kcPushCenterUi','src/ui/push-center.js?v=0.19.43',document.body)}
  function loadEmailCenter(){if(window.KCDP?.emailCenter||document.getElementById('kcEmailCenterUi'))return;script('kcEmailCenterUi','src/ui/email-center.js?v=0.19.57',document.body)}
  function loadSessionMobileHotfix(){if(window.KCDP?.sessionMobileHotfix||document.getElementById('kcSessionMobileHotfix'))return;script('kcSessionMobileHotfix','src/ui/session-mobile-hotfix.js?v=0.19.58',document.body)}
  function loadUxPolish(){css('kcUxPolishCss','src/ui/kc-ux-polish.css?v=0.19.55');if(window.KCDP?.kcUxPolish)return;script('kcUxPolishJs','src/ui/kc-ux-polish.js?v=0.19.55',document.body)}
  function loadMobileColleagueSearch(){script('kcMobileColleagueSearchJs','src/ui/mobile-colleague-search.js?v=0.19.51',document.body)}
  function loadWishPhaseGuard(){script('kcWishPhaseGuardJs','src/ui/wish-phase-guard.js?v=0.19.52-safe-7',document.body)}
  function loadExtras(){loadPushCenter();loadEmailCenter();loadSessionMobileHotfix();loadUxPolish();loadMobileColleagueSearch();loadWishPhaseGuard();setTimeout(ensureMailSettingsTab,0)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',loadExtras,{once:true});else loadExtras();
})();