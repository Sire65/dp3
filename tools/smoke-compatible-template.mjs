import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import vm from 'node:vm';
const context=vm.createContext({window:{},document:{readyState:"complete"},TextDecoder,console});for(const f of ['src/core/model.js','src/adapters/wish-import.js','src/ui/compatible-template.js'])vm.runInContext(await readFile(f,'utf8'),context);const K=context.window.KCDP,csv=K.compatibleTemplate.text();assert(csv.startsWith('\ufeff'));const matrix=K.wishImport.parseDelimited(csv);const header=matrix[0];assert.equal(header.length,11);assert.equal(matrix.length,K.days.length+1);matrix[1][1]='11:00';matrix[1][2]='18:00';matrix[1][3]='12:00';matrix[1][4]='16:00';const result=K.wishImport.normalizeMatrix(matrix);assert(result.valid);assert(result.entries.some(x=>x.wishType==='available'&&x.start===11));assert(result.entries.some(x=>x.wishType==='preferred'&&x.start===12));console.log('Compatible CSV OK: all matrix columns, dates, Kann/Wunsch and re-import.');
