import assert from 'node:assert/strict';import {createRequire} from 'node:module';const {chromium}=createRequire(import.meta.url)(process.env.PLAYWRIGHT_MODULE),b=await chromium.launch({channel:'chrome',headless:true});
try{const p=await b.newPage(),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:8774/twinkey-test.html');await p.locator('[data-tw-mode=guided]').click();await p.locator('[data-tw-task=wish]').click();assert.equal(await p.locator('[data-day]').count(),13);await p.locator('[data-day="2026-12-04"]').click();assert.match(await p.locator('#swTimeline').innerText(),/12:00–18:00 Uhr · 6 h/);await p.locator('[data-day="2026-12-05"]').click();assert.match(await p.locator('#swTimeline').innerText(),/14:00–18:00 Uhr · 4 h/);await p.locator('[data-day="2026-12-02"]').click();assert.match(await p.locator('#swTimeline').innerText(),/08:00–18:00/);for(const width of [320,390,768,1280]){await p.setViewportSize({width,height:844});assert(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));}await p.locator('#swEditDay').click();assert(await p.locator('#swDayBlock').isVisible());assert.deepEqual(errors,[]);console.log('Calendar: 13 days, selected-day timeline, day bounds, mobile layout and edit entry OK.');}finally{await b.close()}
